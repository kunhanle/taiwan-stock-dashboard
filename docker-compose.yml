services:
  backend:
    image: taiwan-stock-backend
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: always
    env_file:
      - backend/.env
    expose:
      - "8050"
    networks:
      - app-network

  caddy:
    image: caddy:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  caddy_data:
  caddy_config:
